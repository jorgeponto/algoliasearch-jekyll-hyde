source 'https://rubygems.org'

gem 'jekyll', '~> 2.5'

# Seems like I have to manually include my dependencies of dependencies
# otherwise the file will not be loaded and the plugin will just silently fail
gem 'html-hierarchy-extractor', path: '../html-hierarchy-extractor/'

group :jekyll_plugins do
  gem 'algoliasearch-jekyll', path: '../algoliasearch-jekyll/'
end
